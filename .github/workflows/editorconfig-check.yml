name: Check .editorconfig

on:
  push:
    branches:
      - TestBranch
      - Geometry
  pull_request:
    branches:
      - main

jobs:
  check-editorconfig:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0'

      - name: Install EditorConfig Checker CLI
        run: |
          choco install editorconfig-checker-cli

      - name: Run EditorConfig checks
        run: |
          editorconfig-checker --path . --output json
